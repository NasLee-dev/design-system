import { vars } from "@ds/themes";

export const VideosPopularList = () => {
  return (
    <section
      style={{
        color: vars.colors.$scale.gray[900],
      }}
    >
      <h2>목록</h2>
    </section>
  );
};
